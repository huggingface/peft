# Pipelines

PEFT pipelines are a way to abstract the call to PEFT models very easily. The pipelines are also easily customizable, by adding the possibility to load the base model in 4bit or 8bit.

## PeftTextGenerationPipeline

[[autodoc]] peft.pipeline.PeftTextGenerationPipeline
    - all