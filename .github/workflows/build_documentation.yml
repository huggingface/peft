name: Build documentation

on:
  push:
    branches:
      - main
      - doc-builder*
      - v*-release

permissions: {}

jobs:
   build:
    # skip on forks, the secrets used below only exist in the main repo
    if: github.repository == 'huggingface/peft'
    uses: huggingface/doc-builder/.github/workflows/build_main_documentation.yml@1b16dac5e33043af565fdf4c1b5b0fe81d0891c8  # main
    with:
      commit_sha: ${{ github.sha }}
      package: peft
      notebook_folder: peft_docs
    secrets:
      token: ${{ secrets.HUGGINGFACE_PUSH }}
      hf_token: ${{ secrets.HF_DOC_BUILD_PUSH }}
